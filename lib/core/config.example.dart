// Copy this file to config.dart and fill in your API keys.
// config.dart is gitignored — never commit real keys.
class AppConfig {
  // Get your key from https://console.cloud.google.com/
  // Enable "Cloud Translation API" then create an API Key under Credentials.
  static const String googleTranslateApiKey = 'YOUR_GOOGLE_TRANSLATE_API_KEY';
}
